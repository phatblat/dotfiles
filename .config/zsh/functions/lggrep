# lggrep - Grep through lightweight log `lg` for a regex pattern
function lggrep() {
    local pattern="$1"
    if [[ -z "$pattern" ]]; then
        echo "Usage: lggrep '.*'"
        return 1
    fi

    lg -G "$pattern"
}

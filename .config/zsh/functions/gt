#!/usr/bin/env zsh

# gt - Run gastown commands via ~/.local/bin/gt
# In Claude Code sessions, pass through to the binary on PATH
[[ -n "$CLAUDE_CODE" ]] && {
    command gt "$@"
    return
}
(cd ~/gt && ~/.local/bin/gt "$@")

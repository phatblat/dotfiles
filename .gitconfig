[github]
    user = phatblat
[alias]
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[branch]
    autosetuprebase = always
[color]
    ui = true
    sh = auto
    branch = auto
    diff = auto
    status = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[color "sh"]
    branch = yellow reverse
    workdir = blue bold
    dirty = red
    dirty-stash = red
    repo-state = red
[push]
    default = simple
[diff "l10n"]
    textconv = iconv -f utf-16 -t utf-8
[core]
	pager = diff-so-fancy | less --tabs=4 --RAW-CONTROL-CHARS --quit-if-one-screen --no-init --pattern '^(Date|added|deleted|modified): '
[hub]
    protocol = ssh
[diff]
    tool = Kaleidoscope
    algorithm = patience
[diff "plistbuddy"]
    textconv = plistbuddy -c Print
[difftool]
    prompt = false
[difftool "Kaleidoscope"]
    cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[difftool "FileMerge"]
    cmd = /Applications/FileMerge.app -left \"$LOCAL\" -right \"$REMOTE\"
[merge]
    tool = Kaleidoscope
[mergetool]
    keepBackup = false
    prompt = false
[mergetool "Kaleidoscope"]
    cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
    trustExitCode = true
[mergetool "FileMerge"]
    cmd = /Applications/FileMerge.app -left \"$LOCAL\" -right \"$REMOTE\" -ancestor \"BASE\" -merge \"MERGED\"
[filter "media"]
    clean = git-media-clean %f
    smudge = git-media-smudge %f
[rerere]
    enabled = true
# Rewrite git:// URLs to use https:// scheme - this breaks an ObjectiveGit test
#[url "https://"]
#    insteadOf = git://
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[credential]
    helper = osxkeychain
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22

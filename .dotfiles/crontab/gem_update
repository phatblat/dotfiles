#-------------------------------------------------------------------------------
#
# crontab/gem_update
# Updates all ruby gems
#
#-------------------------------------------------------------------------------

. $HOME/.dotfiles/crontab/crontab.env

gem_path=`which gem`
$gem_path update --system --quiet && $gem_path update --quiet

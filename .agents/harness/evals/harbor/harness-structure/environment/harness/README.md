# Harness Directory

This directory is intentionally empty. The actual harness snapshot lives in `../snapshot/` (gitignored).

The `../snapshot/` directory is populated at eval-run time by the `just harness-evals` recipe from the live harness at `.agents/harness/`.

# Models

<!-- Generated by scripts/agent-harnesses.py; do not edit directly. -->

4 capabilities. Index: [docs/agent-harnesses.md](../agent-harnesses.md)

### Not yet researched

- `models.effort` · p1 — A shared skill or agent's model and reasoning-effort intent maps to each harness's own model configuration.
- `models.provider_config` · p1 — Which provider and endpoint a model alias resolves to is explicit in that harness's config, not an undocumented default.
- `models.selection` · p1 — The default model and the per-session override mechanism are known per harness.
- `models.small_model` · p2 — Whether the harness has a separate cheap/fast model role, and what it is used for.

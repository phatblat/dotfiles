[default]
region = us-west-2
output = text
# sso_start_url:URL that points to Ditto's AWS SSO user portal.
sso_start_url = https://d-9067b1daed.awsapps.com/start
# sso_region: AWS Region that contains the AWS SSO portal host.
sso_region = us-east-1
# sso_account_id: AWS account ID that contains the IAM role that you want to use with this profile.
sso_account_id = 273076074331
# sso_role_name: name of the IAM role that defines the user's permissions when using this profile.
sso_role_name = SDKPublisher
# sso_role_name = Engineer

[profile ci_account]
region = us-east-1
output = json

[profile ditto-website]
region = us-east-1
output = json

[profile ditto-ci]
# sso_start_url:URL that points to Ditto's AWS SSO user portal.
sso_start_url = https://d-9067b1daed.awsapps.com/start
# sso_region: AWS Region that contains the AWS SSO portal host.
sso_region = us-east-1
# sso_account_id: AWS account ID that contains the IAM role that you want to use with this profile.
sso_account_id = 273076074331
# sso_role_name: name of the IAM role that defines the user's permissions when using this profile.
sso_role_name = SDKPublisher
# sso_role_name = Engineer
region = us-east-1
output = json

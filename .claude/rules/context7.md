# Library Documentation

For library/framework/SDK/API questions, use the `find-docs` skill (`ctx7` CLI) instead of answering from training data — even for well-known libraries. API details, signatures, and configuration options in training data are frequently outdated.

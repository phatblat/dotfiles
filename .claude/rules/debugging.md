# Debugging

- Never propose a fix without first investigating root cause
- After 3+ failed fix attempts, stop and question the architecture
- One change at a time — don't bundle fixes
- Trace data flow through component boundaries before proposing solutions

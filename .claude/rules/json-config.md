---
paths:
  - ".config/zed/settings.json"
  - ".config/cmux/cmux.json"
  - ".claude/policy-limits.json"
---

# JSONC Config Files

These files are JSONC (JSON with comments) despite the `.json` extension — do not reformat with `jq` or strict JSON tooling; use the `format-json` just recipe, which handles them correctly.
